{
  "main": "index.js",
  "scripts": {
    "start": "micro -p ${PORT:-3000}",
    "dev": "micro-dev -p ${PORT:-3000}",
    "format": "prettier *.js --write --single-quote",
    "lint": "eslint --fix **.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=8.x"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "got": "^9.1.0",
    "json2csv": "^4.2.1",
    "micro": "^9.1.0",
    "microrouter": "^3.0.0"
  },
  "devDependencies": {
    "eslint": "^5.4.0",
    "micro-dev": "^3.0.0",
    "prettier": "^1.14.2"
  }
}
